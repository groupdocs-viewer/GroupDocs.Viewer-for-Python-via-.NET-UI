[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "groupdocs-viewer-net-ui"
version = "26.4.0.post1"
description = "Web UI for groupdocs-viewer-net — mount into FastAPI/Flask/Django, or run standalone."
readme = "README.md"
requires-python = ">=3.9"
license = "MIT"
authors = [{ name = "GroupDocs", email = "developers@groupdocs.com" }]
keywords = ["groupdocs", "viewer", "document", "ui", "web", "fastapi", "starlette"]
classifiers = [
  "Development Status :: 5 - Production/Stable",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.9",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Programming Language :: Python :: 3.14",
  "Framework :: AsyncIO",
  "Topic :: Software Development :: Libraries :: Python Modules",
  "Topic :: Office/Business",
]
dependencies = [
  "groupdocs-viewer-net>=26.4.0",
  "starlette>=0.37",
  "pydantic>=2.0",
  "typer>=0.12",
  "uvicorn>=0.29",
  "asgiref>=3.7",
  # Required for the /upload-file endpoint — Starlette's request.form()
  # raises at runtime without it. Not a dev-only dep.
  "python-multipart>=0.0.9",
  # Required for /upload-file's URL-fetch mode and used by the test client.
  "httpx>=0.27",
  # Pydantic resolves field type annotations at runtime via `get_type_hints()`,
  # which on Python 3.9 cannot evaluate `X | Y` (PEP 604 syntax). The backport
  # patches pydantic to handle the modern syntax — installed only on 3.9.
  "eval_type_backport>=0.2; python_version<'3.10'",
]

[project.optional-dependencies]
s3    = ["aioboto3>=12"]
azure = ["azure-storage-blob>=12.25", "aiohttp>=3.9"]
redis = ["redis>=5.0"]
all   = ["groupdocs-viewer-net-ui[s3,azure,redis]"]
dev = [
  "pytest>=8",
  "pytest-asyncio>=0.23",
  "httpx>=0.27",
  "ruff>=0.4",
  "mypy>=1.10",
  "fakeredis>=2.20",
]

[project.scripts]
groupdocs-viewer-ui = "groupdocs_viewer_ui.cli:main"

[project.urls]
Homepage = "https://products.groupdocs.com/viewer/python-net"
Repository = "https://github.com/groupdocs-viewer/GroupDocs.Viewer-for-Python-via-.NET-UI"
"Bug Tracker" = "https://github.com/groupdocs-viewer/GroupDocs.Viewer-for-Python-via-.NET-UI/issues"
Documentation = "https://docs.groupdocs.com/viewer/python-net/"

[tool.hatch.build.targets.wheel]
packages = ["src/groupdocs_viewer_ui"]

[tool.ruff]
line-length = 100
target-version = "py39"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "UP", "B", "A", "C4", "SIM", "RUF"]
ignore = ["E501"]

[tool.ruff.lint.per-file-ignores]
# cli.py:
#   B008 — Typer's canonical pattern uses `typer.Option(...)` and `Path.cwd()`
#          as argument defaults; the rule is incompatible with the framework.
#   UP045 — Typer calls `typing.get_type_hints()` to introspect the signature,
#          which fails on Python 3.9 for PEP-604 `X | None` syntax. Forced to
#          stay on `Optional[X]` until the 3.9 floor moves.
"src/groupdocs_viewer_ui/cli.py" = ["B008", "UP045"]

[tool.mypy]
python_version = "3.9"
strict = true
ignore_missing_imports = true

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
